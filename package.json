{
  "name": "",
  "scripts": {
    "prettier": "$(npm bin)/prettier",
    "sass": "$(npm bin)/sass",
    "browserify": "$(npm bin)/browserify",
    "watchify": "$(npm bin)/watchify",
    "postinstall": "make scss && make js"
  },
  "dependencies": {
    "@sentry/browser": "^5.17.0",
    "browserify": "latest",
    "sass": "latest"
  },
  "devDependencies": {
    "prettier": "latest",
    "watchify": "latest"
  }
}
